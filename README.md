# sproeierserver-hardware

The hardware-part of SproeierServer.

## Setup

1. Copy `settings_example.h` to `settings.h` and modify them to your needs.
2. Open in the Arduino IDE and flash to your NodeMCU

TODO: wiring diagram
